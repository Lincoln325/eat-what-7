# Flat, symlink-free node_modules. pnpm's default symlinked layout makes
# Vercel's serverless packager fail ("files in symlinked directories") when
# outputFileTracingIncludes bundles sharp's native @img/* binaries. A hoisted
# layout keeps those binaries as real files so tracing + packaging both work.
node-linker=hoisted
